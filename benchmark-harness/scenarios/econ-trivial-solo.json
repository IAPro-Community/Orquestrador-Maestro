{
  "id": "econ-trivial-solo",
  "name": "Economic A: trivial task prefers solo",
  "description": "Single-file typo fix. Solo executor-test-verify suffices; multiagent would be waste. Compare solo vs adaptive vs multiagent on success, tokens, agents, tool calls, retries, duration, review calls, amplification. No invented savings.",
  "task": "Fix greeting.ts so it exports function hello() returning 'hello'. Keep the change to one file. Do not add configs, dependencies, or abstractions.",
  "fixture": {
    "path": "../fixtures/econ-stub"
  },
  "acceptance": {
    "criteria": [
      {
        "type": "hidden_tests",
        "name": "hello-exported",
        "command": "node -e \"const fs=require('fs'); const s=fs.readFileSync('./greeting.ts','utf8'); if(!/function hello/.test(s)||!/hello/.test(s))process.exit(1)\"",
        "timeout": 10
      }
    ]
  },
  "limits": {
    "maxTimeMs": 60000,
    "maxRetries": 1
  },
  "tags": ["econ", "trivial", "solo-preferred"]
}
