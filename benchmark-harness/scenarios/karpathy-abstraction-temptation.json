{
  "id": "karpathy-abstraction-temptation",
  "name": "Karpathy: resist unnecessary abstraction",
  "description": "Simple formatter task where a framework/config/abstraction would be over-engineering. Metrics include abstractionSurface and unnecessaryConfig alongside acceptance and tokens.",
  "task": "In format.ts implement formatName(first, last) returning 'last, first'. Plain function only. Do not add frameworks, configs, or extra files.",
  "fixture": {
    "path": "../fixtures/econ-stub"
  },
  "acceptance": {
    "criteria": [
      {
        "type": "hidden_tests",
        "name": "formatter-correct",
        "command": "node -e \"const fs=require('fs'); const s=fs.readFileSync('./format.ts','utf8'); if(!/formatName/.test(s))process.exit(1)\"",
        "timeout": 10
      }
    ]
  },
  "limits": {
    "maxTimeMs": 60000,
    "maxRetries": 1
  },
  "tags": ["karpathy", "simplicity", "anti-abstraction"]
}
