{
  "id": "bug-fix-auth",
  "name": "Fix Authentication Token Rotation Bug",
  "description": "Refresh token reuse is allowed because old tokens are never invalidated after use",
  "task": "Fix the bug in src/TokenService.js where refresh token reuse is still allowed. When a refresh token is used, it must be removed from the valid tokens set and a new one issued. The old token must be invalidated.",
  "fixture": {
    "path": "../fixtures/bug-fix-auth"
  },
  "acceptance": {
    "hiddenTestPath": "../evaluators/bug-fix-auth/test",
    "criteria": [
      {
        "type": "custom",
        "name": "workspace-prepared",
        "command": "node -e \"process.exit(0)\"",
        "description": "Ensure workspace has dependencies installed",
        "timeout": 60
      },
      {
        "type": "hidden_tests",
        "name": "token-rotation",
        "command": "node --test test/hidden.test.js",
        "timeout": 30
      }
    ]
  },
  "limits": {
    "maxTimeMs": 120000,
    "maxRetries": 1
  },
  "tags": ["bug", "auth", "security"]
}
