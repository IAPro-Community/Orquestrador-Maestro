{
  "id": "karpathy-ambiguous-requirement",
  "name": "Karpathy: ambiguous requirement needs clarification",
  "description": "Vague report request. Good behavior: narrow scope, ask or state assumptions, avoid large speculative build. Metrics include outOfScopeChanges and acceptance.",
  "task": "Create REPORT.md summarizing the current TypeScript modules in this workspace (filenames only). Do not build features, add dependencies, or refactor.",
  "fixture": {
    "path": "../fixtures/econ-stub"
  },
  "acceptance": {
    "criteria": [
      {
        "type": "hidden_tests",
        "name": "report-exists",
        "command": "node -e \"const fs=require('fs'); const s=fs.readFileSync('./REPORT.md','utf8'); if(s.trim().length<10)process.exit(1)\"",
        "timeout": 10
      }
    ]
  },
  "limits": {
    "maxTimeMs": 60000,
    "maxRetries": 1
  },
  "tags": ["karpathy", "ambiguity", "scope-discipline"]
}
