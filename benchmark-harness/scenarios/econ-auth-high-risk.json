{
  "id": "econ-auth-high-risk",
  "name": "Economic D: small high-risk auth change",
  "description": "Small but security-sensitive auth change. Independent review must still run; parallelism is not the goal. Metrics: review calls, verification, acceptance, tokens.",
  "task": "In auth.ts ensure login() compares password hashes with a timing-safe comparison and never logs the password. Keep the change minimal and add no dependencies.",
  "fixture": {
    "path": "../fixtures/econ-stub"
  },
  "acceptance": {
    "criteria": [
      {
        "type": "hidden_tests",
        "name": "auth-hygiene",
        "command": "node -e \"const fs=require('fs'); const s=fs.readFileSync('./auth.ts','utf8'); if(!/timingSafeEqual|timing-safe/i.test(s))process.exit(1); if(/console\\.log.*password/i.test(s))process.exit(1)\"",
        "timeout": 10
      }
    ]
  },
  "limits": {
    "maxTimeMs": 120000,
    "maxRetries": 2
  },
  "tags": ["econ", "high-risk", "review-required"]
}
