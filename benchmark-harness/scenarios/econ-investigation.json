{
  "id": "econ-investigation",
  "name": "Economic E: broad investigation",
  "description": "Map error handling across the codebase and report findings in FINDINGS.md. Bounded investigate-then-report; adaptive lanes only if disjoint areas emerge.",
  "task": "Survey *.ts files for try/catch usage and write FINDINGS.md listing files with and without error handling. Do not refactor code, only report.",
  "fixture": {
    "path": "../fixtures/econ-stub"
  },
  "acceptance": {
    "criteria": [
      {
        "type": "hidden_tests",
        "name": "findings-exist",
        "command": "node -e \"const fs=require('fs'); const s=fs.readFileSync('./FINDINGS.md','utf8'); if(s.trim().length<20)process.exit(1)\"",
        "timeout": 10
      }
    ]
  },
  "limits": {
    "maxTimeMs": 120000,
    "maxRetries": 1
  },
  "tags": ["econ", "investigation", "adaptive"]
}
