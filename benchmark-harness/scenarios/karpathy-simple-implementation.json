{
  "id": "karpathy-simple-implementation",
  "name": "Karpathy: simple implementation without overhead",
  "description": "Straightforward utility with no ambiguity. Metrics: acceptance, verification, tokens, agents, retries. Correct minimal implementation wins; extra scaffolding penalized via unexpectedFilesChanged.",
  "task": "In slug.ts implement slugify(text) lowercasing, trimming, replacing spaces with dashes, removing non-alphanumeric except dashes. Single file, no dependencies.",
  "fixture": {
    "path": "../fixtures/econ-stub"
  },
  "acceptance": {
    "criteria": [
      {
        "type": "hidden_tests",
        "name": "slugify-present",
        "command": "node -e \"const fs=require('fs'); const s=fs.readFileSync('./slug.ts','utf8'); if(!/slugify/.test(s))process.exit(1)\"",
        "timeout": 10
      }
    ]
  },
  "limits": {
    "maxTimeMs": 60000,
    "maxRetries": 1
  },
  "tags": ["karpathy", "simplicity"]
}
