{
  "id": "bug-cross-file-001",
  "name": "Fix Cross-File Authentication Bug",
  "description": "Fix a bug that requires understanding interaction between 3+ files",
  "task": "Fix the bug in the authentication middleware where token validation fails when the user has been recently migrated. The issue is that the migration service updates the user record but the token service still uses the old schema for validation.",
  "fixture": {
    "path": "../fixtures/bug-cross-file-001"
  },
  "acceptance": {
    "hiddenTestPath": "../evaluators/bug-cross-file-001/test",
    "criteria": [
      {
        "type": "custom",
        "name": "workspace-prepared",
        "command": "node -e \"process.exit(0)\"",
        "description": "Ensure workspace has dependencies installed",
        "timeout": 60
      },
      {
        "type": "hidden_tests",
        "name": "cross-file-fix",
        "command": "node --test test/hidden.test.js",
        "timeout": 30
      }
    ]
  },
  "limits": {
    "maxTimeMs": 120000,
    "maxRetries": 1
  },
  "tags": ["bug", "auth", "cross-file"]
}
