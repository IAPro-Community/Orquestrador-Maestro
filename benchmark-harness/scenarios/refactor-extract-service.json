{
  "id": "refactor-extract-service",
  "name": "Extract Email Notification Service from Monolith",
  "description": "Extract email notification logic from a monolithic file into a separate service",
  "task": "Refactor src/monolith.js by extracting the email notification logic into a separate EmailService class in src/services/EmailService.js. Update all callers and add tests.",
  "fixture": {
    "path": "../fixtures/refactor-extract-service"
  },
  "acceptance": {
    "criteria": [
      {
        "type": "custom",
        "name": "workspace-prepared",
        "command": "test -d node_modules || npm install 2>/dev/null; exit 0",
        "description": "Ensure workspace has dependencies installed",
        "timeout": 60
      },
      {
        "type": "hidden_tests",
        "name": "service-extracted",
        "command": "node --test test/hidden.test.js 2>&1",
        "timeout": 30
      }
    ]
  },
  "limits": {
    "maxTimeMs": 120000,
    "maxRetries": 1
  },
  "tags": ["refactor", "extract", "service"]
}
