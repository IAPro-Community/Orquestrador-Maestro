{
  "id": "bug-context-noise-001",
  "name": "Fix Email Validation in Large Codebase",
  "description": "Bug is in a large codebase with many irrelevant files",
  "task": "Fix the bug in src/core/validator.js where email validation rejects valid international email addresses. Ignore all other files in the project — only src/core/validator.js and its tests need to change.",
  "fixture": {
    "path": "../fixtures/bug-context-noise-001"
  },
  "acceptance": {
    "hiddenTestPath": "../evaluators/bug-context-noise-001/test",
    "criteria": [
      {
        "type": "custom",
        "name": "workspace-prepared",
        "command": "node -e "process.exit(0)"",
        "description": "Ensure workspace has dependencies installed",
        "timeout": 60
      },
      {
        "type": "hidden_tests",
        "name": "email-validation",
        "command": "node --test test/hidden.test.js",
        "timeout": 30
      }
    ]
  },
  "limits": {
    "maxTimeMs": 120000,
    "maxRetries": 1
  },
  "tags": ["bug", "validation", "email", "noise"]
}
