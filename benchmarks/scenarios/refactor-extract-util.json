{
  "id": "refactor-extract-util",
  "name": "Extract Duplicated Date Logic to Shared Utility",
  "type": "refactor",
  "version": 1,
  "difficulty": "medium",
  "tags": ["refactor", "dedup", "utility", "shared-code"],
  "timeoutMs": 120000,
  "resources": { "cpu": "1", "memory": "512m" },
  "description": "ReportService duplicates date formatting logic that exists in dateUtils",
  "prompt": "Refactor src/services/ReportService.js to import and use the shared date utility functions from src/utils/dateUtils.js instead of duplicating the logic. Add parseDate and daysBetween functions to dateUtils.js if they don't exist. Remove the duplicated formatDate, addDays, and isWeekend functions from ReportService.js.",
  "fixtureDir": "_fixtures/refactor-extract-util",
  "hiddenTests": "test/hidden.test.js",
  "acceptance": [
    "ReportService imports from dateUtils",
    "No duplicated formatDate function in ReportService",
    "dateUtils exports parseDate and daysBetween",
    "All existing tests still pass"
  ],
  "validation": {
    "command": "node --test test/hidden.test.js",
    "expectedExitCode": 0,
    "timeoutMs": 30000
  },
  "expectedInvariants": [
    "Single source of truth for date operations",
    "No code duplication between modules"
  ]
}
