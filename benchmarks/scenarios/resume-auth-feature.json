{
  "id": "resume-auth-feature",
  "name": "Complete Authentication Service Implementation",
  "type": "feature",
  "version": 1,
  "difficulty": "medium",
  "tags": ["auth", "session", "password-reset", "feature"],
  "timeoutMs": 120000,
  "resources": { "cpu": "1", "memory": "512m" },
  "description": "AuthService has TODO items for password reset and session refresh",
  "prompt": "Complete the AuthService implementation in src/AuthService.js. The service has TODO comments for password reset and session refresh functionality. Implement: 1) A passwordReset method that generates a reset token and stores it with an expiry, 2) A refreshSession method that extends an existing session's expiry, 3) Ensure all edge cases are handled (expired tokens, invalid sessions, etc).",
  "fixtureDir": "_fixtures/resume-auth-feature",
  "hiddenTests": "test/hidden.test.js",
  "acceptance": [
    "Password reset generates a valid reset token",
    "Reset token expires after configured time",
    "Session refresh extends session expiry",
    "Invalid sessions are rejected"
  ],
  "validation": {
    "command": "node --test test/hidden.test.js",
    "expectedExitCode": 0,
    "timeoutMs": 30000
  },
  "expectedInvariants": [
    "All auth flows handle edge cases",
    "Sessions have proper expiry"
  ]
}
